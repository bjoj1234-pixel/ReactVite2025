import { useContext } from "react";

export default function Loginform(){
    const


    return(
        <div>
            <h2>로그인</h2>
            <div>
                <input type="text" />
                <button type="button">로그인</button>
            </div>
        </div>
    )
}